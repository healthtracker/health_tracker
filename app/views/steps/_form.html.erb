<%= form_for(@step) do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :steps %><br>
    <%= f.number_field :steps %>
  </div>
  <div class="field">
    <%= f.label :stepped_on %><br>
    <%= f.date_select :stepped_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
